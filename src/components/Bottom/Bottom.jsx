import PlayerMenu from "./Players/PlayerMenu";

const Bottom = () => {
  return (
    <section className="h-50 bg-white">
      <PlayerMenu />
    </section>
  );
};

export default Bottom;
